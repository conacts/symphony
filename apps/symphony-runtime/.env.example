NODE_ENV=development
PORT=4400

# When running the runtime from apps/symphony-runtime, point back at the repo workflow.
WORKFLOW_PATH=../../WORKFLOW.md

# Keep runtime artifacts out of the app directory.
SYMPHONY_RUN_JOURNAL_FILE=../../.symphony/run-journal.json

# Required for tracker polling.
LINEAR_API_KEY=
