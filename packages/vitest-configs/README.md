# @symphony/vitest-configs

Shared Vitest configuration presets for packages.

Current base preset includes:
- Node environment defaults
- automatic mock restoration/clearing
- shared coverage provider/report defaults (`v8`, text/json/lcov)
